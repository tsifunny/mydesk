# 02 · 多想一步 · LLM 约束 v2.4

> 适用产品：MyDesk Next 920-test  
> `prompt_version`：`plain-speak-v2.4`  
> 本文件取代 v2.3 中关于「条件 Agent」「候选条件」「不支持形态」和 `[加为条件]` 的设计。v2.3 留作历史记录，不再作为实现依据。

## 0. 这版到底改了什么

这版把三个原本混在一起的职责彻底拆开：

1. **判断转译**只整理用户原话，不看行情，不提取机械条件。
2. **多想一步**只在写卡当时，用冻结的日线证据把含糊说法讲清楚，并指出最多两条真正的技术面冲突。
3. **行情进展**在用户以后每次打开或刷新时，重新读取最新已收盘日线，沿着用户原话中的 thesis 继续观察，并判断它是「正在观察」还是「已经出了结果」。

直接删除以下旧设计：

- 「改变判断的条件」下拉框和手工阈值编辑器；
- LLM 返回的 `conditions`、`suggestions`、`unsupported`、`next_question`；
- `[加为条件]`、`[采用建议]` 等操作；
- 仅因为用户用了清单外术语，就判定「无法判断」；
- 没写认错条件时，单独提醒或要求用户补写认错条件；
- K 线图下方的单日涨跌、近 5 天涨跌、近 20 天涨跌、MA20 偏离数据条。

旧卡已有的条件数据只用于兼容历史记录，不得回填到新卡，也不得重新出现在新建界面。

---

## 1. 产品承诺

用户写下的不是一组待执行规则，而是一段有时间、有背景的市场判断。

系统必须做到：

- 写下时保存原话和当时的已收盘日线快照；
- 下次打开或刷新时，扫描所有写过 thesis 的标的；
- 拿最新已收盘日线与写卡时点对照；
- 沿着用户原话里真正关心的信号复核，而不是只匹配几个预设关键词；
- 市场还没给答案时继续观察；
- 市场已经给答案时说明什么时候、由什么证据给出的；
- 判断得到验证时可以明确说「恭喜」；
- 判断被市场否定时自动复盘当时哪里看错；
- 错过时点、没有采取行动或机会已经过去，不自动等同于「判断失败」。

## 2. 总链路

```text
用户原话
  ├─ A. 判断转译：只润色文字、识别立场
  ├─ B. 多想一步：冻结 T0 数据，给出可验证读法和技术面异议
  └─ 保存判断卡：原话 + 采用后的表述 + T0 快照 + 读法
                         ↓
下次打开 / 刷新：读取最新已收盘日线与技术信号
                         ↓
按原话逐项复核：用户定义优先，通行技术定义其次
                         ↓
              正在观察 / 已经出了结果
```

A 与 B 是两次相互独立的调用。任一调用失败都不得阻止另一调用，也不得阻止用户保存原话。

---

## 3. A · 判断转译契约

### 3.1 输入

转译模型只能收到：

```json
{
  "user_text": "用户原话"
}
```

不得发送：

- 标的代码、公司名；
- 当前价格或任何行情快照；
- 涨跌幅、均线、波动率；
- 历史条件或系统建议；
- 系统对这段判断的结论。

原因：这一步只负责表达，不允许模型根据行情悄悄改变用户的 thesis。

### 3.2 输出

只接受：

```json
{
  "professional": "忠于原意的专业表述",
  "stance": "bullish | bearish | neutral | wait_condition"
}
```

任何额外返回的 `conditions`、`suggestions`、`unsupported`、`next_question`、`role` 或阈值都必须丢弃，不得展示、不得保存。

### 3.3 硬约束

- 只整理术语与句式，不添加用户没说过的观点、数字、阈值或认错条件。
- 不改变方向，不把等待写成看多，不把看多写成买入建议。
- 用户写了什么信号就保留什么信号。
- 清单外术语不能被删除、替换或近似成清单内指标。
- 用户给了自己的定义，必须连同定义保留。
- 用户没给定义，也保留原词，交给后续行情进展按通行技术含义复核。
- 一句话，尽量不超过 40 字；同一件事只说一次。

### 3.4 无模型时的降级

本地规则只做两件事：

1. 从原话推断四种立场之一；
2. 收紧多余空白，原样保留用户文本。

规则版不得假装完成专业改写，不得制造条件、阈值或替代术语。

### 3.5 用户选择

界面只提供：

- `采用`
- `保留原话`

无论用户选择哪一个，`raw_input` 都必须永久保留原始输入，后续行情进展不能只看润色后的句子。

---

## 4. B · 多想一步契约

### 4.1 时间边界

多想一步只看用户写卡时已经收盘的日线，记为 T0。

- 不使用盘中未收盘 K 线；
- 不使用 T0 之后的数据；
- 不调用模型自己的市场记忆；
- 不读取新闻、财报、估值或公司背景；
- 非日线页面明确显示「仅日线可用」，不伪造结果。

### 4.2 模型能看到的证据

模型只可使用输入中 `status: ok` 的 evidence 行。常用证据包括：

- T0 收盘价；
- 单日、近 5 天、近 20 天涨跌；
- 20 天年化波动和单日正常波动；
- 20 天均线、价格相对均线的位置、连续位于均线上下方的天数、均线斜率；
- 用户图上已启用且数据可用的 5 天、10 天均线；
- 近 20 天低点及距低点天数。

这些数据用于模型理解和代码计算，不再以一整排指标卡放在 K 线图下面。

### 4.3 输出

```json
{
  "readings": [
    {
      "quote": "用户原话中的片段",
      "kind": "move_band | close_vs_ma | no_new_low | ma_turn",
      "params": {},
      "note": "不超过16字"
    }
  ],
  "objections": [
    {
      "point": "不超过24字",
      "cites": ["evidence_id"]
    }
  ]
}
```

这四种 `kind` 只是写卡当下「把含糊话说具体」所允许的计算模板，不是行情进展的知识边界，更不是用户可以使用的技术概念白名单。

### 4.4 readings

- 总计最多 4 条；同一原话片段最多 2 种不同读法。
- `quote` 必须能在用户原话中找到。
- 用户已经写明数字、价位、百分比或天数的片段不再翻译。
- `kind` 和参数只能来自本次根据可用证据裁剪后的字典。
- 模型不得写任何计算结果；价位、幅度和天数一律由代码计算。
- `note` 最长 16 字；没有补充价值就留空。
- `note` 含买卖建议、数字、套话或系统没有的事实时清空，但不因此删除整条 reading。
- 重复读法直接去重。

### 4.5 objections

- 最多 2 条，可以为 0 条。
- 只写与用户立场真正冲突的技术事实，中性事实不能硬说成冲突。
- 每条最长 24 字，一句话只说一件事。
- 每个提到的指标和方向都必须能由 `cites` 指向的 evidence 证明。
- 不写数字；界面在下方展示引用证据的实际值。
- 不写买卖建议、胜率、目标价、宏观、财报、新闻或本次没有提供的指标。
- 不得使用「其实」「也就是说」「需要注意」「从技术面来看」等铺垫和套话。
- 没有真正冲突时，界面显示「技术面没看到冲突」。

### 4.6 数值换算

代码负责所有数值。模型只选语义档位。

`move_band`：

```text
σ_h = vol_20d × √(h / 365)
幅度 = max(1.0%, round_to_0.5(k × σ_h))
k = small: 0.5, mid: 1.0, large: 2.0
```

- 有可用裁决周期时，可保存为到期按收盘价核对的目标读法。
- 没有可用裁决周期时只显示换算，标记「暂不自动核对」。

`close_vs_ma`：

- 展示均线周期、当前均线值、要求连续天数和当前是否满足；
- 不再转成独立机械条件；
- 标记「按原话复核」。

`no_new_low` 与 `ma_turn`：

- 只作为原话的具体读法保存在卡片中；
- 不生成隐藏条件。

### 4.7 校验原则

代码校验只允许：

- 删除不合格条目；
- 清空不合格 `note`；
- 去重；
- 用确定性公式补上计算结果。

代码不得替模型改句子，也不得把未通过的内容改造成另一条条件。

### 4.8 界面动作

多想一步的内容只有一个动作：`记下来`。

它的含义是把这条可验证读法随判断卡保存，供以后复核；不是「加为条件」，也不代表系统替用户设定了触发交易的阈值。

---

## 5. 保存契约

新卡至少保存：

```json
{
  "raw_input": "用户原话，永不覆盖",
  "judgment_text": "用户最终采用的表述",
  "stance": "bullish | bearish | neutral | wait_condition",
  "snapshot": "T0 已收盘日线快照",
  "precise_readings": [],
  "conditions": [],
  "conditions_confirmed": true
}
```

说明：

- 新卡的 `conditions` 必须为空数组。
- `conditions_confirmed` 只是旧引擎兼容字段，不代表用户确认过条件。
- 新卡不再保存 `unsupported_notes`。
- 旧卡已经存在的 `conditions` 不删除，以保证历史页面和旧裁决可回看；但不得复制到新卡。
- 后续行情进展必须同时拿到 `raw_input`、最终表述、T0 快照和已记下的读法。

---

## 6. C · 下次打开后的行情进展

### 6.1 触发时机

以下动作都应触发扫描：

- 用户打开产品；
- 用户刷新产品；
- 用户主动刷新行情；
- 用户打开某张有 thesis 的历史判断卡。

扫描对象是所有存在历史 thesis 的标的，不局限于当前屏幕上的一个标的。

### 6.2 数据要求

每个标的读取：

- T0 之后到最新的已收盘日线；
- T0 与当前的价格和可计算指标变化；
- 常见技术信号及首次出现日期；
- 用户原话中提到的每个 thesis 信号。

数据不足时不能编造结论。应维持「正在观察」，并准确说清缺少哪段行情或哪类数据。

### 6.3 技术概念解释顺序

任何技术概念都按以下优先级解释：

1. **用户定义**：用户在原话中已经说明什么算触发，就严格按用户定义。
2. **通行技术定义**：用户只写了术语时，用常见技术分析定义结合实际 K 线复核。
3. **无法验证**：现有数据确实不足时，标明缺什么，不得因为术语不在内置清单就判「不支持」。

内置信号可包括但不限于：

- 空头排列、多头排列及首次被打破；
- 均线金叉、死叉、均线拐头；
- 十字星、长上影、长下影、避雷针、射击之星；
- 突破、假突破、站稳、跌破；
- 筑底、止跌、反转确认；
- 用户自己命名或定义的形态。

内置清单是计算加速器，不是知识白名单。LLM 可以调用通行技术分析知识解释清单外概念，但必须用实际行情举证，并输出采用的定义和证据日期。

### 6.4 认错条件

- 不单独提供「认错条件」输入框。
- 用户在 thesis 原话中明确写了认错条件，行情进展才核对它。
- 用户没写，就完全跳过这一维度。
- 不提示「没有认错条件」，不要求补写，不因缺失而阻止结论。
- 「严格高于 / 低于」按原义处理；等于阈值不算触发，除非用户原话另有定义。

### 6.5 输出状态

用户界面只保留二分状态：

- **正在观察**：市场还没有给出足以结束这次观察的答案，或数据不足。
- **已经出了结果**：成功、被否定、到期或市场已经给出足以复盘的明确结论。

不再显示「定性观察」「待复盘」「等待中」等并列分类。

### 6.6 结果文案

行情进展至少说明：

- 距上次记录多少天；
- 上次判断原文；
- 真正发生变化的价格、指标和技术信号；
- 用户原话中每个关键信号：采用什么定义、现在是否出现、第一次确认日期；
- 如果已经出结果：结论、关键证据日期和简短复盘。

语气要求：

- 结果与 thesis 一致：可以用「恭喜」开头，明确指出市场如何验证了原判断。
- 结果否定 thesis：直接说明当时哪一部分判断没有成立，以及后来出现了什么反证。
- 只是错过了最佳时点：表述为「机会已经过去」或「这个等待条件后来已经完成」，不要自动说用户判断失败。
- 仍在观察：说清已经发生什么、还缺什么，不使用机械的「条件 1 未触发」。
- 行情进展是研究复核，不是实时买卖指令。

---

## 7. 界面文案

| 场景 | 文案 |
|---|---|
| 未配置模型 | `未配置 LLM` / `当前使用规则转译` |
| 转译按钮 | `转译` / `重新转译` |
| 多想一步入口 | `多想一步` |
| 多想一步标题 | `说清楚 · 非投资建议` |
| 无技术冲突 | `技术面没看到冲突` |
| 多想一步不适用 | `仅日线可用` |
| 当日无数据 | `该日没有日线` |
| 无自动核对能力 | `暂不自动核对` |
| 均线等原话读法 | `按原话复核` |
| 观察未结束 | `正在观察` |
| 观察已结束 | `已经出了结果` |

文案不得暴露内部字段名、校验编号或「机械裁决」字样。

---

## 8. 失败与降级

- 转译失败：保留原话，用规则只推断立场。
- 多想一步失败：显示规则版或空结果，不阻止保存。
- 最新行情失败：保留上次成功结果，明确标记数据截至日期，不拿旧数据冒充最新。
- 某个信号无法计算：只把该信号标为目前无法验证，不连带否定整张卡。
- LLM 返回非法 JSON：可低温重试一次；仍失败则走规则版。
- 所有失败均记录诊断信息，但用户界面只显示能帮助下一步行动的短句。

---

## 9. 验收清单

### 9.1 转译

- [ ] 输入只含用户原话，不含标的和行情。
- [ ] 输出只采用 `professional` 与 `stance`。
- [ ] 旧模型即使返回条件、建议和 unsupported，也全部丢弃。
- [ ] 「我画的蓝线」「箱体二次确认」等自定义概念原样保留。
- [ ] 未配置 LLM 时不伪造专业改写。

### 9.2 多想一步

- [ ] 只使用 T0 已收盘日线。
- [ ] note 超过 16 字或含套话时清空。
- [ ] objection 超过 24 字或证据引用不完整时删除。
- [ ] 模型不能直接写价格、百分比和阈值。
- [ ] 均线读法不再生成独立条件。
- [ ] 页面没有 `[加为条件]`。
- [ ] K 线图下没有四格指标条。

### 9.3 保存与兼容

- [ ] 新卡 `conditions: []`。
- [ ] 新卡不保存 `unsupported_notes`。
- [ ] `raw_input` 不因采用转译而丢失。
- [ ] 老卡的历史条件仍能读取。

### 9.4 行情进展

- [ ] 每次打开或刷新扫描所有写过 thesis 的标的。
- [ ] 只使用最新已收盘日线。
- [ ] 用户定义优先于通行定义。
- [ ] 清单外概念会结合行情判断，而不是报不支持。
- [ ] 用户没写认错条件时完全不评价这一项。
- [ ] 状态只显示「正在观察 / 已经出了结果」。
- [ ] 成功、被否定和错过时点使用不同复盘语义。

---

## 10. 版本迁移

从 v2.3 升级到 v2.4：

1. 停止向转译模型发送快照和指标。
2. 停止解析与保存 `conditions`、`suggestions`、`unsupported`、`next_question`。
3. 删除新建卡页面的条件编辑器和所有采纳条件按钮。
4. 新卡固定保存空 `conditions`；旧卡只读兼容。
5. 把清单外概念交给后续行情进展解释，不在写卡时拒绝。
6. 把多想一步的字数限制收紧为 note 16 字、objection 24 字。
7. 页面状态统一为「正在观察 / 已经出了结果」。
8. prompt 版本升级为 `plain-speak-v2.4`，避免旧缓存与新契约混用。

本契约的优先级：用户本次明确要求 > 本文件 > 历史 v2.3 及更早文档。
