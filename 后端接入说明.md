# MyDesk：rMETA 修复与 Bitget 后端接入

## 本次修复

- 允许删除最后一只关注标的；清空后不再自动恢复默认列表。
- 删除当前 Meta 后，立即切换到剩余标的并重载对应图表。
- 取消关注保留历史研究卡；行情缺失时显示空值，不使用其他标的的价格。
- 首次切换到真实行情时定位到最新已收盘日线，避免沿用演示日期导致图表为空。
- 无效 OHLC 数据不会进入图表；部分标的拉取失败时仍保留该标的的选择与删除入口。
- 旧网络请求不会覆盖新关注列表；刷新失败时保留上次数据并标明失败。

## Agent Hub

后端使用官方 `@bitget-ai/bitget-agent-sdk`。页面通过
`GET /api/agent-hub/rtokens?symbols=rMETA&days=120` 获取代币化股票数据。

“添加标的”中的 **rToken 选股器** 会请求
`GET /api/agent-hub/rtoken-universe`，读取当前在线的完整 rToken 目录，支持按代码、公司名和
预置分类筛选后加入研究空间。页面中的行情与 K 线仍只读取对应 rToken；Bitget MCP 的连接状态和
可用工具独立显示，供研究资料扩展使用。

rMETA 固定对应 Bitget 现货 `RMETAUSDT`。目录须满足 `areaSymbol=yes`、`status=online`，
不会把 META 原股价格作为 rMETA 的替代数据。

rToken 行情由 SDK 的 `publicGet` 调用现货 v2 目录、日线和报价接口；
SDK 的标准 v3 `market` 工具也通过后端开放，参数可用 `discover` 查询。

图表可选 1 小时、4 小时和日 K。小时线通过独立的展示接口读取，不进入研究条件的计算；
保存判断时，卡片会同时保留 `chart_interval`（用户当时查看的周期）和
`condition_interval: "1D"`（条件与复核的固定日线口径），避免跨周期混用。

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/agent-hub/tools` | 列出 SDK 的公开工具和输入格式 |
| GET | `/api/agent-hub/rtoken-universe` | 取得 Bitget 当前在线的完整 rToken 目录，供选股器使用 |
| GET | `/api/agent-hub/rtoken-chart?symbol=rMETA&interval=4H&limit=360` | 取得 rToken 的 1H / 4H / 1D 展示 K 线；只接受 rToken 代码 |
| POST | `/api/agent-hub/call` | 调用 `market` 或 `discover` |
| GET | `/api/agent-hub/rtokens?symbols=rMETA&days=120` | 取得 rMETA 日线、报价与引擎数据源 |

调用示例：

```json
{"name":"discover","arguments":{"tool":"market","action":"candles"}}
```

```json
{"name":"market","arguments":{"action":"candles","category":"SPOT","symbol":"RMETAUSDT","interval":"1D","limit":"100"}}
```

SDK 配置仅启用公开行情，不载入交易凭证，不开放交易或资金接口。

## Bitget MCP Server

使用官方 MCP SDK，经 Streamable HTTP 连接 `https://agent.bitget.com/mcp`，
支持初始化、工具发现、工具调用、失败后重连。

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/api/bitget-mcp/tools` | 首次使用时连接远端，并列出实际返回的工具 |
| POST | `/api/bitget-mcp/call` | 调用目录中可用的参考资料工具 |
| GET | `/api/integrations/status` | 查看两套服务的配置与最近连接结果，不发出外部请求 |

调用格式为 `{"name":"从 tools 目录取得的名称","arguments":{}}`。
不要猜测工具名称或参数，应先读取实际目录。

该 MCP 的原股价格类接口不用于 rToken 图表。后端对参考资料回复明确标记
`assetClass: underlying-reference`、`usableAsTokenPrice: false`。
页面行情只走 Agent Hub 的 rToken 数据源。

## 启动与代理

Windows x64 便携包自带 Node.js 与依赖。完整解压、先开启 VPN，再双击 `启动MyDesk.cmd`。
源码版需要 Node.js 24.16 或更高版本及完整依赖，也可运行 `npm start`。
启动器自动打开 `http://127.0.0.1:4173/startup`（端口被占用时自动另选），不要用双击 HTML 的方式启动后端功能。
连接检查页自动验证 Hub、MCP 与真实日线，再由用户进入研究工作台。请保持启动窗口运行。

`MYDESK_PROXY_URL=auto` 默认自动检测 Windows 系统代理、HTTP/HTTPS/ALL_PROXY 环境变量与直连/全局隧道。
若均失败，再识别常见本机代理端口及系统代理进程的其他监听端口。
Hub 和 MCP 分别选用通过真实接口验证的线路；明确填写代理地址时只使用该线路。
本机请求不经代理，TLS 证书校验保持开启。浏览器扩展代理不一定覆盖本机服务。
连接检查页支持手动代理与重新识别，改动只对本次启动生效。

如需配置，将 `.env.example` 复制为 `.env` 后填写；`.env` 不对浏览器开放。
设置 `MYDESK_USE_SYSTEM_PROXY=0` 可停用自动读取 Windows 代理；
`MYDESK_PROXY_DISCOVERY=0` 停用第 1 层（按进程名识别本机代理 + 常见代理端口）；
`MYDESK_PROXY_WIDE_SCAN=0` 停用第 2 层（全端口兜底扫描，仅在前两层全灭时启动）。

三层递进的顺序：手动指定 / 系统代理（含 PAC）/ 环境变量 / 直连 → 按进程名识别的本机代理与常见端口
→ 全端口兜底扫描。每层内部用 `Promise.any` 竞速，通过的标准是**带字段校验的探测**
（`code === '00000'` 且 `data.serverTime` 存在；MCP 要真返回 tools 数组），不是「返回 200」。

排障用 `诊断连接.cmd`（等价于 `node tools/doctor.cjs`）：12 段报告会说明
「试过哪些线路、分别怎么失败」，并落盘《诊断报告-<时间戳>.txt》。
`npm run doctor` 同效；加 `--no-probe` 只做本机侧发现、不真的连 Bitget。

## 验证记录与当前状态

2026-09-18 重新实测：经本机系统 HTTP 代理连通 Hub 与 MCP，返回 2 个 MCP 工具，
并取得 RMETAUSDT 的 30 根日线。单独指定本机 SOCKS5 代理也通过连接验证；本机直连遇到 ECONNRESET。
已验证独立解压目录与包内 Node；清除代理环境变量后能自动读取当前系统代理。
已实际运行 Windows 命令脚本并修复换行兼容与 ESM 依赖检测问题。
这些结果仅代表当前机器与当前节点，尚未在第二台实体电脑验证；VPN 节点必须允许访问两个 Bitget 域名。
公开行情不需要账号 Key；可选 AI 转译仍需配置对应模型服务。

可主动运行 `npm run check:backend` 检查正在运行的本机服务，或在页面点击“连接检查 / 更换代理”。

官方参考：[Agent Hub](https://github.com/Bitget-AI/agent_hub)、
[Agent SDK](https://github.com/Bitget-AI/agent-sdk)、
[黑客松手册](https://bitget-ai.gitbook.io/bitgetai_hackathons2/base-camp-hackathon-s2-cn)。
